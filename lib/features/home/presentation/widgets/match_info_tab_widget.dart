import 'package:flutter/material.dart';

class MatchInfoTabWidget extends StatelessWidget {
  const MatchInfoTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Match info ', style: TextStyle(color: Colors.black)),
    );
  }
}
